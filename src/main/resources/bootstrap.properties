spring.application.name=sampleProps

spring.profiles.active=dev
#spring.cloud.config.label=sit

spring.cloud.config.uri=http://localhost:8888
management.security.enabled=false

#App startup fails if config server is down
#Refresh event will error out however app will still work with old properties
spring.cloud.config.fail-fast=true